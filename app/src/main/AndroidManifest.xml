<?xml version="1.0" encoding="utf-8"?>
<!-- This is the manifest file for the application. It contains information about the application, such as its package name, permissions, and activities. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- The application element contains all of the components of the application. -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LensQuest1">
        <!-- This is the main activity of the application. It is the first screen that the user sees when they launch the app. -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- This activity is for signing in or signing up. -->
        <activity
            android:name=".Sign"
            android:exported="false" />
        <!-- This is the main activity of the application. -->
        <activity
            android:name=".MainActivity"
            android:exported="false">
        </activity>
    </application>

</manifest>